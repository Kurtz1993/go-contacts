import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kurtz1993.contacts',
  appName: 'Contacts',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
